import SetStateAlise from '../aliases/SetStateAliase';
import GridStateType from './GridStateType';

type StateType = {
  state: GridStateType;
  setState: SetStateAlise;
};

export default StateType;
